# script to run in the background which will periodically check logs for errors
# should run in background and check that server is up and not in lock state 
# if in lock state should send report email.
